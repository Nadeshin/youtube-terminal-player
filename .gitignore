# auto-downloaded on first run (see installer.js) — never commit binaries
bin/*.exe
bin/*.7z
bin/*.extract/

# build artifact (rebuild via tools/build-exe.ps1) — shipped as Release asset, not git
/yt-player.exe

# deps & logs
node_modules/
*.log
npm-debug.log*

# OS junk
.DS_Store
Thumbs.db
